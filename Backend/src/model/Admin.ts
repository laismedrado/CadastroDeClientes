export type createAccountAdmin = {
  id?: string;
  name: string;
  email: string;
  password: string;
};
export type authAccountAdmin = {
  id?: string;
  email: string;
  name: string;
  password: string;
};
